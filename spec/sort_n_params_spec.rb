require 'spec_helper'
require 'sort_n_params'
require 'pry'

RSpec.describe SortNParams::SortHelpers, type: :helper do
  describe 'SortHelper' do
    before(:all) do
      @user = create(:user)
    end
  end
end
